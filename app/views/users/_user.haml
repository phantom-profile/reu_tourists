%tr[user]
  %td= user.firstname
  %td= user.lastname
  %td= user.email
  %td= user.is_admin?
  %td= user.created_at
  %td
    = link_to_unless user.is_admin?, "Destroy", user_path(user), method: :delete,
      remote: true,
      data: { confirm: "Are you sure?" }
