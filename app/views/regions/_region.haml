%tr[region]
  %td= region.nearest_city
  %td= region.country
  %td= region.country_code
  - for_elder_only do
    %td
      = link_to "Edit", edit_region_path(region), remote: true
      = link_to "Destroy", region_path(region), method: :delete, remote: true, data: { confirm: "Are you sure?" }